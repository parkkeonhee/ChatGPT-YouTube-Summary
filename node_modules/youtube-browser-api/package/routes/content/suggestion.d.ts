import { type Page } from '../data/parse';
export declare function getCompactVideoRenderer(videoId: string): Promise<{
    id: string;
    type: string;
    thumbnail: import("../data/types/initial-data").Thumbnail5[];
    title: string;
    channelTitle: string;
    shortBylineText: string;
    length: import("../data/types/initial-data").LengthText;
    isLive: boolean;
}[]>;
export declare function getNextResult(page: Page): import("../data/types/initial-data").TwoColumnWatchNextResults;
