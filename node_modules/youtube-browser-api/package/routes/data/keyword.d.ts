import type { ITEM } from '../types';
export declare function GetListByKeyword({ keyword, withPlaylist, limit, option, }: {
    keyword: string;
    withPlaylist?: boolean;
    limit?: number;
    option?: 'video' | 'channel' | 'playlist' | 'movie' | '';
}): Promise<{
    items: ITEM[];
    nextPage: {
        nextPageToken: string | undefined;
        nextPageContext: {
            context: import("./types/initial-data").Context;
            continuationToken: string;
        };
    };
}>;
