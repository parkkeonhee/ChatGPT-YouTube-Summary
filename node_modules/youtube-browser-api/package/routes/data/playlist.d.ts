import type { ITEM } from '../types';
export declare const GetPlaylistData: (params: {
    playlistId: string;
    limit: number;
}) => Promise<{
    items: ITEM[];
    metadata: import("./types/initial-data").MetaData;
} | undefined>;
